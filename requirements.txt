# Production dependencies for Hugging Face Spaces / Docker deployment.
# Versions are pinned to known-good ranges; tighten further if reproducibility
# becomes a citation concern.

biopython>=1.83,<2.0
flask>=3.0,<4.0
gunicorn>=21.2,<23.0
