# Production dependencies for Hugging Face Spaces / Docker deployment.
# Pins are tight enough to avoid surprise breakage when HF's Docker cache
# expires and picks up a newer minor release; loose enough that dependabot-
# style patch updates still land automatically.

biopython>=1.83,<1.86
flask>=3.0.3,<3.2
gunicorn>=22.0,<24.0
werkzeug>=3.0,<3.2  # transitively required by Flask; pin to avoid a mismatch surprise
